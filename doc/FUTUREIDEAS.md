> > Both of the use cases and the way the component was designed was to facilitate people shooting handheld. I would like to revisit this component in the future to add in considerations for using a tripod. This would allow me to factor in use cases like astrophotography as well as allowing the user to input longer shutter times.

> > This project was created on the base of the OSU CSE components, so eventually I'd like to transition the code here to the JCF data structures and interfaces.

> > Because this project works essentially as a light meter, but is currently coded as a proof-of-concept within OSU CSE frameworks, it's not going to be great with fufilling it's purpose(a light meter should be portable). As such, I'd either like to figure out a way to port this to a mobile/web app or integrate microcontroller programming to make a seperate handheld device.

> > Lastly, linking a database to this to store previous balance advices with locations and other metadata could be helpful after this is turned into a mobile/web app or seperate hardware. It would allow the user to take their old data and tweak settings so that they can go from a "perfect" exposure to one that fits their creative style.
